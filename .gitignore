# Browser profile data (Playwright/Chromium)
agent/.tms_browser_profile/

# Python environment
agent/venv/
agent/.venv/
__pycache__/
*.pyc

# API keys — NEVER commit these
.env
agent/.env
agent/gdrive_credentials.json
agent/.gdrive_token.json
agent/.qbo_tokens.json
desktop/build-env/

# Windows junk
Thumbs.db
desktop.ini
$RECYCLE.BIN/
*.lnk

# OS junk
.DS_Store

# Large files — prevent accidental commits
*.zip
*.rar
*.7z
*.tar.gz
*.mp4
*.mov
*.avi
*.iso
*.exe
*.msi

# Editor / IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# Logs and temp files
*.log
*.tmp
*.temp
send-errors-*.txt

# Node
node_modules/

# Desktop build output
desktop/dist/
desktop/agent-dist/
desktop/build-temp/

# Runtime data — machine-specific, regenerated automatically
agent/data/ngl.db
agent/data/ngl.db-shm
agent/data/ngl.db-wal
agent/data/ngl_accounting.db
agent/data/ngl_accounting.db-shm
agent/data/ngl_accounting.db-wal
agent/data/*.bak
agent/data/do_sender_cache.json
agent/data/archive/

# Claude Code workspace (personal tools)
.claude/

# Transient build logs
desktop/build-log*.txt

# Debug screenshots and HTML captures
agent/debug/

# Merged output PDFs
agent/output/

# Data backups (local only)
agent/backups/
