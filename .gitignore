# Browser profile data (Playwright/Chromium)
agent/.tms_browser_profile/

# Python environment
agent/venv*/
agent/.venv/
__pycache__/
*.pyc

# API keys — NEVER commit these
.env
agent/.env
agent/gdrive_credentials.json
agent/.gdrive_token.json
agent/.qbo_tokens.json
desktop/build-env/

# Windows junk
Thumbs.db
desktop.ini
$RECYCLE.BIN/
*.lnk

# OS junk
.DS_Store

# Large files — prevent accidental commits
*.zip
*.rar
*.7z
*.tar.gz
*.mp4
*.mov
*.avi
*.iso
*.exe
*.msi

# Editor / IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# Logs and temp files
*.log
*.tmp
*.temp
send-errors-*.txt

# Node
node_modules/

# Desktop build output
desktop/dist/
desktop/agent-dist/
desktop/build-temp/

# Runtime data — machine-specific, regenerated automatically
agent/data/ngl.db
agent/data/ngl.db-shm
agent/data/ngl.db-wal
agent/data/ngl_accounting.db
agent/data/ngl_accounting.db-shm
agent/data/ngl_accounting.db-wal
agent/data/*.bak
agent/data/do_sender_cache.json
agent/data/archive/

# Claude Code workspace (personal tools)
.claude/

# Transient build logs
desktop/build-log*.txt
desktop/build-log*.err
desktop/publish-log*.txt
desktop/publish-log*.err
desktop/*.err

# Build scaffolding (PowerShell empty-stdin helpers)
desktop/empty-stdin.txt
desktop/.empty-stdin

# Local superpowers workspace
.superpowers/

# Customer / billing data (never commit — keep outside repo)
docs/*.xlsx

# Debug screenshots and HTML captures
agent/debug/

# Merged output PDFs
agent/output/

# Data backups (local only)
agent/backups/
